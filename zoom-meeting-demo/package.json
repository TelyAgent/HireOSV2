{
  "name": "zoom-meeting-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Minimal, dependency-free (besides @zoom/rtms) closed-loop RTMS test harness. No NestJS, no Prisma, no app code — isolates whether RTMS failures are ours or Zoom's.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@zoom/rtms": "^1.1.0"
  }
}
