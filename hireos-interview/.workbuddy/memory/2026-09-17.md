# 2026-09-17 RTMS 收尾日

## 上午确认（13:00 前）
- **真实 RTMS 转录已端到端跑通**：DB 里有两场真实中文转写（Round1 b48aaedf 8 行 David Kim；Round2 3b5bbd6b 9 行 Sarah Chen，今天上午 10:39-10:45 北京时间），说话人分离正确。付费账号 + client_id 修复后链路已通。
- 昨晚 20:14 起 watch 进程卡 11 个编译错误 → 根因：schema.prisma 更新（ParseJob.roundId 等）后没跑 `prisma generate`。已 generate，tsc 0 错误。
- 用户换到**新的付费 Zoom 组织**，env 全部换成新 app 凭据：ZOOM_OAUTH_PUBLIC_CLIENT_ID=yKC6...、ZM_RTMS_CLIENT=LGpy...（=ZOOM_MEETING_SDK_CLIENT_ID）、ZM_RTMS_SECRET=1BcB...、ZM_RTMS_WEBHOOK_SECRET=Y5Ul...（旧值被注释）。

## 用户误解纠正
- 用户把 Event Subscription 建成了 **WebSocket 方式**（wss://ws.zoom.us/ws?subscriptionId=K_pQSg89StGaza-_Cyk62g）——这不是 webhook，不能写进 env，后端不会主动连它。RTMS 事件官方文档要求 webhook POST handler。已指导改成 Webhook 方式 + ngrok endpoint。
- 存储的 Zoom 授权 token 仍是旧 app（SNF7ys，已过期）→ 必须用新组织账号重新 OAuth（会走 yKC6 新 app）。

## 后端重启（13:12）
- 杀掉旧进程 86428 + 僵尸 66637/66674，task 6wQmce 后台启动 start:dev，stdout >> .local/api.log。PID 57540，启动 0 错误，transcript API 正常。
- rtms.service.ts 的启动 client_id 已用 ZOOM_OAUTH_PUBLIC_CLIENT_ID（新 app = yKC6）。

## 待验证
- 新组织开会 → webhook(Webhook方式) 收 meeting.started → 自动 PATCH(带 yKC6) → rtms_started → SDK join(LGpy+secret 签名) → 入库。

## 14:05 用户新建全新 Zoom app "HireOS Interview v2"，已更新 .env
- 新 app 凭据（付费新组织）：
  - Client ID `zZNZhiIlTWQsnTptW8_0Q` → ZOOM_MEETING_SDK_CLIENT_ID + ZM_RTMS_CLIENT
  - Client Secret `wH8s6siJ7xgHbTMEgoODLSVtkvmJvEBt` → ZOOM_MEETING_SDK_CLIENT_SECRET + ZM_RTMS_SECRET
  - Public Client ID `vmEwl1t6QoKI0o6oPjlbAQ` → ZOOM_OAUTH_PUBLIC_CLIENT_ID
  - Event Subscription Secret `1X9Rr-NMT2K4kEAgdd-aYQ` → ZM_RTMS_WEBHOOK_SECRET
- 关键映射（已从源码确认）：
  - ZOOM_OAUTH_PUBLIC_CLIENT_ID = REST 启动 client_id + OAuth 授权（zoom-host.service.ts:43）
  - ZM_RTMS_CLIENT/SECRET = @zoom/rtms SDK 信令握手签名，SDK 内部读 process.env，签名 HMAC-SHA256(client_id,meeting_uuid,rtms_stream_id,secret)
  - ZOOM_MEETING_SDK_CLIENT_ID/SECRET = 前端 Meeting SDK JWT 签名（meetings.service.ts sign()）
  - ZM_RTMS_WEBHOOK_SECRET = webhook 验签
- 已重启后端（PID 64607，0 错误，transcript API 正常）。
- 存储账号仍是旧 app（clientId=SNF7ys，已过期）→ status() 会判定未连接，必须重新 OAuth。

## 14:20 新 app 授权成功 + scope 排查
- **授权已跑通**：账号文件已更新为新 app（clientId=vmEwl1t6...，苏俊杰），token 有效。
- 实测三个端点的 scope 需求（用真实 token 探针 .local/probe-scopes.mjs）：
  - GET /users/me → 200（user:read 已加，之前 ZOOM_PROFILE_FAILED 的根因）
  - GET /meetings/{id} → 200（meeting:read:meeting 已有）
  - POST /users/me/meetings → 400 code 4711：缺 `meeting:write:meeting`（当前 ZOOM_CREATE_FAILED 根因）
- **新 app 完整 scope 清单（6个）**：user:read、meeting:read:meeting、meeting:write:meeting、meeting:read:meeting_audio、meeting:read:meeting_transcript、meeting:update:participant_rtms_app_status
- 后端调用映射（zoom-host.service.ts / rtms.service.ts）：
  - GET /users/me → user:read
  - POST /users/me/meetings → meeting:write:meeting
  - GET /meetings/{uuid} → meeting:read:meeting
  - PATCH /live_meetings/{id}/rtms_app/status → meeting:update:participant_rtms_app_status
- 注意：新加 scope 后**必须重新授权**（token 的 scope 在授权时固化）。
- 观察：profile 显示 user `type:1`（Basic），role Admin——用户称新组织是付费会员，但该用户类型仍为 Basic，需留意 RTMS 是否受计划影响。
- 后端日志问题：nest watch 重编译 fork 新子进程后 stdout 不再写入 .local/api.log（旧父进程持有重定向），日志停在 14:05。排查需靠探针脚本。

## 14:24 关键结论：加 scope 后必须重新授权
- 用户加了 meeting:write:meeting 但仍报 ZOOM_CREATE_FAILED。探针验证：token iat=14:19:08（加 scope 之前签发），POST /users/me/meetings 仍返回 4711。
- **根因：scope 固化在授权时签发的 token 里，加 scope 后必须重新走 OAuth**。
- 前端重新授权入口：ZoomHostPanel.tsx 第44行，已连接时按钮显示"重新授权/Reconnect"，调 POST /api/meetings/host/authorize 拿 authorizationUrl 后 window.open。
- 后端 authorize 用 loopback+PKCE：每次生成随机端口（127.0.0.1:0），redirect=http://127.0.0.1:{port}/api/integrations/zoom/callback。Zoom 后台只需注册 `http://127.0.0.1/api/integrations/zoom/callback`（不带端口，loopback 匹配忽略端口）。
- WorkspaceGuard 身份硬编码 {workspaceId:'local-workspace', actorId:'local-user'}（workspace.guard.ts:15），故 curl 生成的授权 URL 与前端等价。
- 后端已改为 `npm run start`（非 watch）启动，PID 67617，日志恢复写入 .local/api.log（watch 模式 fork 后日志会断）。
- 探针脚本：.local/probe-create.mjs、.local/probe-scopes.mjs、.local/probe-check.mjs

## 14:28 Meeting SDK 3712（Signature is invalid）根因
- 现象：新 app 授权成功、status connected=true（meeting 81822123493），但内嵌 Meeting SDK 入会报 `join · 3712 · Signature is invalid`。
- 排查：后端签发的 Meeting SDK JWT 已用 .env secret 验签通过、appKey===clientId、payload 正常（mn 数字会议号/role 0/iat/exp/tokenExp）→ **后端签名逻辑无误**，.env 无隐藏空格。
- 根因（官方论坛 Zoom 工程师 chunsiong.zoom）：**General App 的 Meeting SDK 开关处于 off** → 3712。
  - 官方原话："if you are getting error code 3712, there is a possibility that on your General App, Embed Tab, the meeting SDK toggle button is currently in the 'off' state"
  - 另一帖："create a General App instead of legacy Meeting SDK app. Under the embed tab, turn on the 'meeting sdk' toggle switch. You can now use the clientID and clientSecret for your meeting sdk application."
  - "after enabling meeting SDK in the embed tab, use Client ID as SDK Key and Client Secret as SDK Secret."
- **修复路径**：Marketplace → 新 app → **Features → Embed** → 打开 **Meeting SDK** 开关 → 保存。无需改 .env（同一对 Client ID/Secret 即 SDK Key/Secret），无需重新 OAuth（Meeting SDK 签名本地生成）。
- 附带：开启 Meeting SDK 会自动添加 `user:read:zak` scope（代码以 role 0 入会，ZAK 非必需但会自动加）。
- Meeting SDK 前端：public/zoom-meeting/meeting.js 用 Zoom Embedded SDK 6.2.0，join({meetingNumber,password,userName,signature})；签名来自 POST /api/meetings/dev/join-config 或 host/start（meetings.service.ts sign()）。

## 14:33 决定性结论：RTMS 被 2310 拦截 = 账号仍是 Basic（未真正切到付费组织）
- 真实链路已触发：14:32:54 收到**真实** Zoom webhook `meeting.started`（ua=Zoom Marketplace/1.0a），会议 89811062176（=round dd83db72，"HireOS Interview — Round 1"，scheduledAt 14:25）。后端自动 PATCH 启动 RTMS。
- **Zoom 返回 400 / code 2310 "Failed to perform RTMS app operation"**（非 13262 了）。
- 对照实验（.local/probe-rtms2.mjs，会议 89811062176 实时）：
  - client_id=Public Client ID `vmEwl1t6...` → **400/2310**
  - client_id=App Client ID `zZNZhiIl...` → **403/13262**
  → 证实：**内容授权绑定在 Public Client ID 上**（OAuth/PKCE 用的那个），代码用 ZOOM_OAUTH_PUBLIC_CLIENT_ID 正确。
- 官方 troubleshooting（developers.zoom.us/docs/rtms/troubleshooting/）原文：
  - **2310 含义**："RTMS start operation was rejected. HTTP 400 with error code 2310." 常见原因：① 账号无有效 Developer Pack 订阅 ② Developer Pack 试用过期 ③ **账号类型为 Basic，不支持 RTMS**
  - **Plan compatibility 表**：Basic(Free)=Not supported（"RTMS webhooks will not fire; API calls will fail with error 2310"）；Pro/Business/Enterprise=Supported，且**都需 Developer Pack 订阅**
- **关键证据**：`GET /v2/users/me`（.local/probe-plan.mjs）→ **type: 1 = Basic**，account_id `uWu5DoxuTImyCnXqH6fMRA`，role Admin，pmi 2082885735。host_id `6H_dziZYRL2GSu3zNo47og`、host_email **sujunjie427@gmail.com**（与旧账号完全一致）。
- **结论**：当前 OAuth 授权的账号仍是 **Basic 免费账号**（type 1），并非付组织。用户以为切到了付费组织，但授权实际落在旧 gmail 账号上。Zoom 官方 support 原文印证："If you have a Pro account, but your account is showing Zoom Workplace Basic, **you may not be signed in to the correct account**."
- 待用户做：确认付费组织账号 email → 彻底登出 zoom.us / 用无痕窗口 → 登录**付费组织账号** → 前端重新授权 → 核对 /users/me 变 type 2 → 再开会验证。
- 新增探针：`.local/probe-plan.mjs`（plan/type）、`.local/probe-rtms2.mjs`（会议详情+双 client_id 对照）。
- 状态：round dd83db72 idle 0 行；其余 round：b48aaedf(ended,8行/9-16)、3b5bbd6b(ended,9行/9-17 02:45)，均为旧数据。

## 14:41 RTMS 权益归属模型（官方 + Recall.ai 证实）——用户问"必须用付费账号授权吗？我的 app 是给所有人用的"
- **官方**：developers.zoom.us/docs/rtms/meetings/add-features → 开头即 "**Any developer with Zoom Developer Pack credits on their account can use RTMS**."；Prereq 只有 "Zoom Developer Pack credits on your account"。pricing 页："**All add-ons require a paid base plan**"。
- **关键澄清（devforum 145295，Amanda @ Recall.ai = Zoom RTMS Preferred Partner 官方口径）**：
  - "Zoom requires the **app developer** to have credits and require a participant-initiated REST start to target an invited participant who has joined."
  - "**Zoom staff has confirmed that Basic hosts are supported when the RTMS developer account has Developer Pack entitlement.**"
  - Publication 只在"外部用户要安装/授权"时才必需（需 App Review）。
  - "OAuth scopes do not override customer controls. Admins can disable Share realtime meeting content with apps at account/group/user level, and hosts can still require approval or deny access."
- **角色分工结论**：
  - **App 所有者（开发者）账号** = RTMS 权益 + 计费主体，必须 **付费基础计划(Pro+)** + **Developer Pack 积分**。额度从这扣。
  - **客户账号** = 各自 OAuth 授权自己的账号、各自当主持人；**计划可以是 Basic**，不需要自己的 Developer Pack。
- **对外分发**：developers.zoom.us/docs/rtms/meetings/submit-app-review → "Getting the app approved is **required before your app can be added by users outside your Zoom account**"；选项 Public(Listed)/Unlisted/Beta/Private。账号内用户免审（Apps on account → Users & Groups → +Add Users）。
- **REST 启动硬前提（再次确认）**：participant 必须是 **meeting invitee 且已入会**。
- **对当前 2310 的含义**：2310 说明 **App 所有者账号**不满足"付费计划 + Developer Pack"。所以要确认 v2 app(zZNZhiIl/vmEwl1t6) 到底建在哪个账号下：若是 sujunjie427@gmail.com(Basic) → 客户再付费也没用。
- **架构提示（已告知用户）**：当前后端单租户（.local/zoom/ 只存 1 个账号 token）；要"给所有人用"须改多租户（按用户存 token + 按用户路由 webhook/round），并过 App Review。成本上 RTMS credits 从开发者账号扣（客户开会、你付费）。

## 14:45 用户提供 Zoom 账号管理截图 → 实锤：组织内用户全是 Basic 许可证
- 截图（abcft.com 组织 → 用户管理）：
  - `it-support@abcft.com` | 角色 **所有者(Owner)** | 许可证 **Zoom Meetings基础版**
  - `sujunjie427@gmail.com` | 角色 **管理员(Admin)** | 许可证 **Zoom Meetings基础版**
  - 下方还有 `rooms_foivJDufRbGw0Z-...`（Zoom Rooms 用户，暗示组织可能有其他付费产品）
- 新建探针 `.local/probe-account.mjs` 实测（用存储 token）：
  - `GET /users/me` → **type:1 (Basic)**、role_name Admin、account_id `uWu5DoxuTImyCnXqH6fMRA`、pmi 2082885735、verified 0
  - `GET /accounts/me` → 400 code 4711，缺 `account:read:sub_account:admin`
  - `GET /users?status=active` → 同缺 `user:read:admin`（无法列用户）
- **重要纠正**：`sujunjie427@gmail.com` **本身就是 abcft.com 组织的管理员**（account_id 一致），并非"授权到了另一个免费账号"。之前"授权账号错了"的判断需要修正——账号没错，是**该账号/该用户没有付费 Meetings 许可证**。
- 结论：2310 的直接原因 = **App 所有者账号（= 该组织账号）没有付费基础计划**（type=1）+ 可能 Developer Pack 也未落在该账号。基本版用户即使被授权为 Admin 也不满足 RTMS。
- 待用户确认：① 组织账单页实际订阅了什么（是否有未分配的付费许可证）② 用户管理页点「更改许可证」看是否有 Pro 可分配 ③ Developer Pack 挂在哪个账号。

## 14:58 账单页截图 → 账号有付费订阅，但是 Zoom Rooms（增项），不是 Meetings 基础计划
- 截图卡片：**Zoom Rooms** · Monthly subscription · Renews Oct 10, 2026 · Quantity **5 rooms** · Renewal Amount **$245.00 → $122.50（50% off coupon）** · Cloud Storage 11 MB of 5 GB。
- 关键判定：**RTMS 要求的是「付费的 Zoom Workplace/Meetings 基础计划（Pro 起）」，Zoom Rooms 属于独立产品线/增项，不满足该前提。**
- 官方依据（已验证）：
  - Zoom 支持文档 KB0063402：部署完整 Zoom Room 需 Zoom Room 许可证（或 30 天试用）；另有「Zoom Room 测试设置」可免许可但功能受限。
  - Zoom 社区（社区冠军 LK_EXN）原文：**"Zoom Rooms license requires at least 1 Zoom One Pro (or higher) license. It does not work with Basic (free) account."**
  → 说明正常情况下该账号应已有 ≥1 个 Pro 席位；但用户列表 + API 均显示全是基础版，存在矛盾，需人工核对账单页「计划」区。
- 新建探针 `.local/probe-billing.mjs`：`GET /accounts/{id}`、`/billing`、`/plans` 均返回 4711，分别缺 `account:read:sub_account:master` / `billing:read:billing_information:master` / `billing:read:plan_information:master` —— **用户级 OAuth 无法读取账户计划，只能靠后台人工核对**。
- `GET /users/me` 补充：created_at 2023-11-06，host_id `6H_dziZYRL2GSu3zNo47og`（与项目最早那个账号一致），timezone Asia/Shanghai，pmi 2082885735，type=1。
- 待用户核对两处：① 账单页顶部「计划」区是否有 Zoom Workplace 专业版 Pro；② 用户管理 → 更改许可证 下拉是否有 Pro 可分配。

## 15:03 定论：账号只有 Zoom Rooms，没有任何 Zoom Workplace 付费基础计划
- 账单「Plan Management」标签页截图证据：
  - 「当前计划」区**只有 Zoom Rooms 一张卡**（Monthly · Renews Oct 10, 2026 · Manage）
  - **Estimated Totals: Monthly $122.50** —— 等于 Rooms 一项，无其他计费项
  - **Workplace 出现在下方「Recommended for you」列表里（From $16.99/month）→ 未购买**（这是最直接证据）
  - 其他推荐项：Phone $12、My Notes $10、Scheduler $5.99（Free Trial）、Workplace Reservation $1.50、Visitor Management $2
- **根因链条完全闭合**：账号无付费基础计划 → 所有用户 license = Basic → `/users/me` type=1 → RTMS PATCH 返回 400/2310（RTMS Plan compatibility：Basic = Not supported）。
- 修正先前推测：Zoom 允许「Rooms 增项 + Meetings Basic 基础计划」并存（此前查到社区说法"Rooms 需 ≥1 Pro"在当前打包下不成立）。之前"账号切到了错误账号"的判断最终被证伪——账号没错，就是这个账号从没买过 Workplace。
- **唯一解法**：在 Plan Management 购买 Zoom Workplace（Pro，From $16.99/mo 月付）≥1 席位并分配给 `sujunjie427@gmail.com` → 账号变为付费基础计划 → RTMS 2310 应消失（仍需 Developer Pack 积分在同一账号）。
- 购买后**无需重新 OAuth**（scope 不变），只需重跑探针确认 type 变 2，再开会验证。
- 成本：$122.50/mo → 约 $139.49/mo（月付 Pro 1 席）；年付 Pro 约 $13.33/用户/月更省。

## 16:04 用户又建了新 app "HireOS"，.env 出现"两个 app 混用"的致命错配
- 用户新建第 4 个 app **HireOS**（Development）：Client ID `rb6B9VAs...`（或 IbBB5VA5TB...，截图小字难辨）、Public Client ID `f5kKWw0E...`；OAuth Redirect URL 已填 loopback `http://127.0.0.1/api/integrations/zoom/callback`，页面上有黄色警告：**"Loopback IP addresses are only supported for PKCE public client OAuth flows. Confidential OAuth clients will not receive this redirect URL."**
- **.env（15:57 用户手改）实况**：ZOOM_OAUTH_PUBLIC_CLIENT_ID 还是**旧 app#3 的 yKC6piRRhM...**，而 ZM_RTMS_CLIENT/ZOOM_MEETING_SDK_CLIENT_ID 已换成新 app 的 IbBB5VA5TB...、webhook secret=b-MRuC... → **OAuth 用的 public client 和 SDK 用的 client 来自两个不同 app**。
- 存储授权文件（14:25，v2 app vmEwl1t6）token 已过期（401/code 124）。后端被用户重启过（现 PID 73278，非我启动，stdout 未接 api.log）。
- **PROFILE 400 (4711 缺 user:read:user:admin/user:read:user) 的真因**：重新授权走的是 env 里的旧 public client（yKC6 app，从未加过 user scope）→ token 无 user scope。**与"是否用 admin 账号授权"无关**。
- 官方证据（Zoom 官方 docs/troubleshooting 原文，16:00 查证）：
  - Plan compatibility 表：**Basic (Free) — Not supported** "RTMS webhooks will not fire; API calls will fail with error 2310"；Pro/Business/Enterprise = Supported（需 Developer Pack）。
  - Error 2310 定义："Account type does not support RTMS (Basic accounts)"；解决："If using a Basic account, upgrade to a paid plan and purchase Developer Pack credits."
  - RTMS 概览页（developers.zoom.us/docs/rtms）原文："To use RTMS, you'll need credits on your account."
- 4711 与 scope 官方解释（devforum 37586, yveszoom）：**GET /users/me 需要的 granular scopes 就是 `user:read:user:admin`（账户级 app）或 `user:read:user`（用户级 app）**；user-managed app 文档明确用 `me` 调用。"必须 admin 授权"是误读——是缺 scope，不是缺 admin 身份。
- "Add for Others → Redirect URL is required (4,700)" 根因：该安装流走**机密客户端**，而机密客户端没有可用 redirect（loopback 仅限 PKCE public client）。修法：在 OAuth Redirect URL 里加 ngrok HTTPS 地址；但当前阶段**不需要 Add for Others**（后端 PKCE loopback 流不需要它，它是账户级安装/auto-start 用的）。
- 新 UI 无 "user-level/account-level" 类型标注；listing 页 **"User role: Any user" + "Zoom account eligibility: No additional requirements"** 即等价于任何用户可授权（user-managed 友好）。
- **收敛方案（已给用户）**：全部统一到最新 HireOS app——① Scopes 加 6 个（user:read / meeting:write:meeting / meeting:read:meeting / meeting:read:meeting_audio / meeting:read:meeting_transcript / meeting:update:participant_rtms_app_status）② Embed → Meeting SDK 开关打开（否则又 3712）③ Event Subscription 用 Webhook 方式配 ngrok endpoint + 3 事件，取新 Secret Token ④ 把 4 个凭据发我 → 统一改 .env → 重启 → 重新授权。

## 16:50-17:00 授权与 RTMS 权益收尾（重大进展）
- **4711 与 marketplace error 均已解决**：换新浏览器授权成功。根因是旧浏览器 cookie/登录态缓存（非 app 配置）。app 确认是 Public Client + PKCE（Use Public Client OAuth 开）。
- **新付费账号到位**：`it-services@sding.me`（Cloud Host Room，Owner，**type=2 Licensed**，account_id `M5uc0junTyaMaYqJqj-zKA`，PMI 4638061757）。买了 Zoom Workplace Pro（3 用户，月付 $25.49）——补上了 2310 的 Basic 根因。
- **当前 app 凭据**：Public Client ID = `t9KmvYkBSVqr2uAQN12MWQ`；SDK client = `IbBB5VA5TBKEF7roSPTTRQ`（secret 在 .env）。scope 已加 9 个（含 rtms:read:rtms_started/stopped、user:read:zak）。
- 建会验证：POST /users/me/meetings 201 成功（会议 84716355443、89731753586）。
- **RTMS 启动当前阻塞 = 2310（Developer Pack 未关联到此付费账号）**：
  - 用 public client_id `t9KmvYk` 启动 → 400/2310 "Failed to perform RTMS app operation"
  - 用 SDK client `IbBB5VA5TB` 启动 → 403/13262（client 未授权访问会议内容，应传 public client id）
  - 官方论坛 chunsiong.zoom 反复确认：2310 = 账号无 Developer Pack 权益；有 credits 仍 2310 需 Zoom 后台手动 provision。
  - 官方新消息：RTMS Self-Service 已上线 https://zoom.us/pricing/developer
- **下一步（待用户）**：把 Developer Pack/RTMS credits 关联到 it-services@sding.me 付费组织（或确认之前的 20 credits 在哪个账号），开通后重新授权再发起 RTMS。
- **遗留架构问题**：后端仍是单账号架构（.local/zoom/ 只存 1 份 token，credits 全从开发者账号扣），"给所有人用"需多租户改造（按用户存 token + 按用户路由 webhook），后续再做。
- 探针新增 `.local/probe-rtms-now.mjs`（列出会议 + RTMS 启动，用 SDK client）；临时探针 /tmp/probe-rtms-correct.mjs（用 public client 启动）。

## 17:00-17:25 Developer Pack 生效后仍无 rtms_started（硬证据）
- 用户已激活 **Zoom Developer Pack | Free Trial（0 of 20 credits, 0% used）** → PATCH 启动行为从 2310 变为 **204**（17:05 起三次均 204）。
- **但 `meeting.rtms_started` 从未到达**。用 ngrok 检查器(127.0.0.1:4040/api/requests/http)解码全部 webhook payload 证实：有记录以来只有 `meeting.started`（16:44/16:53/16:56/17:05/17:09 共 5 次），无一次 rtms_started。
- 基线监测：PATCH 204 后 100 秒内 ngrok **零新增请求** → Zoom 侧根本没推事件。
- 事件订阅确认正确：单订阅 `hireos-rtms`，方法 Webhook，endpoint=ngrok，建议 URL，3 事件（含 RTMS started/stopped in Meeting）。
- 相关 round：`dd83db72-3d6f-4d97-89bd-90cc5ec3026d`（meetingLink=/j/89731753586）transcriptStatus 仍为 **idle**。
- **日志可见性问题**：当前服务 3001 的进程是 node v24.18.0（nvm，用户自己终端启动），stdout 不在 /tmp/hireos-api.log；我的日志文件停在 16:56。以后排查应以 ngrok 检查器 payload 为准。
- **待验证变量**：host 是否需在会中（用户此前以非 host 身份入会）。下一步：用 it-services@sding.me 登录并作为 host 入会后再发起。
- 若仍无事件 → 属 Zoom 侧 provisioning（devforum 同类案例：billing 显示 Pack 已激活但后台未 provision，需 Zoom 手动开通；关键信息 account_id `M5uc0junTyaMaYqJqj-zKA`、client `IbBB5VA5TBKEF7roSPTTRQ`）。

## 17:26-17:40 关键真相：204 是异步受理，不等于成功
- 官方文档明确：`PATCH /live_meetings/{id}/rtms_app/status` 返回 **204 = "request accepted and will be processed asynchronously"**，不是成功。前置条件不满足时**静默失败**，不推 `meeting.rtms_started`。→ 完美解释"204 但零事件"。
- **硬前置（官方）**：只有 **会议 host 或 alternative host** 才能执行；403/2308 "Only the meeting host or an alternative host can perform this action"。且 devforum 实测：**手动 API 启动只在 host 先入会时生效**，非 host 先入会则静默失败（自动启动不受此限）。
- **client_id 结论**：settings.client_id 必须填**被授权访问会议内容的那个 id**。本项目实测：填 public client `t9KmvYkBSVqr2uAQN12MWQ` → 204（被受理）；填 SDK client `IbBB5VA5TBKEF7roSPTTRQ` → 403/13262。→ 说明账号设置的"允许应用访问会议内容"里登记的是 **public client id**。
- 会议 89731753586 于 17:25:11 再次 meeting.started（uuid `zOx3gEEHRc2RjnWOsEHZMQ==`，host_email it-services@sding.me，host_id 613-OuW0RMaHLJfHLFykzg），全程 live。
- **未解**：用户以访客/非 host 身份入会，host 不在会中 → RTMS 静默失败。
- **正确修复路径（官方）**——走 **Auto-start**：
  1. Web 门户 → Account Management → **Account Settings** → **Zoom Apps** 标签
  2. 打开 **"Share realtime meeting content with apps"**
  3. 在 **"Auto-start apps that access shared realtime meeting content"** → **+ Choose an app to auto-start** → 选 RTMS app → Auto-start 设 **On**
  4. 前提：app 必须已 install 给该用户（Add Users / 账号级安装）
  → 自动启动不依赖 host 先入会，且是 SaaS 产品的正确形态（无需手动 PATCH）。
- 另：RTMS 仅对 **user-managed（用户级）app** 可用（admin-managed 看不到 RTMS 事件）——本项目是 user-managed ✅。
- 会话内出现 host 侧 "Share realtime meeting content with apps" 的会议内开关（Host tools），也需为 On。

## 17:34 破解：app 从未被"安装"，所以 auto-start 列表里没有它
- 症状：Account Settings 的 "Share realtime meeting content with apps" 已开，但 **auto-start 下拉里没有自己的 app**；会议内 host 工具里**没有 "Share realtime meeting content with apps" 这一项**（截图确认）。
- **根因（官方 devforum 原话）**：OAuth 授权 ≠ app 安装。Zoom 有**两个不同的 Zoom Apps 设置页**：
  - **User Settings → Zoom Apps** = 只装在"我这个用户"上的 app
  - **Account Settings → Zoom Apps** = 装在"整个账号"上的 app
  - **auto-start 列表只显示与当前所在层级一致的已安装 app**。所以账号级页面看不到只装在用户级的 app。
- 安装方式：
  - 测试最快：Marketplace → 你的 app → **Local Test 页 → "Add app now"**（装到当前用户）
  - 账号级：App listing page → **Install for Others**
  - chunsiong.zoom 原话："You need to **assign users to this app**. For testing purpose, click **'add app now'** in your **Local Test** page."
- 另一条官方提醒（scopes 会因 RTMS 停用而消失）：auto-start 可见需具备 `meeting:read:meeting_audio`、`meeting:read:meeting_transcript`、`rtms:read:rtms_started`、`rtms:read:rtms_stopped` 四项。
- 装好 + auto-start 设 On 后，host 入会即自动推 `meeting.rtms_started`（官方 quickstart 验证路径）。

## 17:45 系统性读完官方 RTMS 文档后的定论（不再猜测）
官方 4 篇：docs/rtms/meetings/add-features、work-with-streams、troubleshooting、quickstart-websockets。

**A. REST 启动路径（我们一直在用的）有两条第 1 项是致命的硬条件 —— 官方 work-with-streams 原文：**
> "Your app can start RTMS for a participant when **both** of the following are true:
> - The participant is an **invitee** to the meeting. An invitee is a participant who was added to the meeting's **invite list**, not a participant who joined with a shared meeting link.
> - The participant has already joined the meeting.
> The host or designated alternate host must be present."

→ 我们所有测试会议都是 `POST /users/me/meetings` 建的，**invitees 为空**（没人被加进邀请名单）。所以 REST PATCH 注定被异步静默拒绝——这解释了"204 + 零事件"，官方 troubleshooting 里对应 **Error 2308**："User joined anonymously using a meeting link instead of being on the invitation list"。

**B. 官方推荐且不受 invitee 限制的路径 = Auto-start（work-with-streams 原文）：**
> "Auto-start is enabled at the account, group, or user level and automatically launches RTMS when a user with auto-start enabled:
> - **Hosts** — RTMS starts automatically when the user **initiates** a meeting or webinar.
> - **Joins as participant** — RTMS starts automatically when the user joins a meeting or webinar."
→ 触发条件只有：**装了 app 的用户本人发起/主持会议**。

**C. 当前已满足项（据用户 17:45 截图）**
- Local Test → Add app（已装，按钮显示 Remove App）✅
- **User Settings → Zoom Apps → Auto-start：列表里有 HireOS，状态 On** ✅
- 用户级与账号级 "Share realtime meeting content with apps" 均 On ✅
- Developer Pack Free Trial（0/20 credits，免费版并发上限 5 路）✅
- app 的 **Client ID = IbBB5VA5TBKEF7roSPTTRQ**（Local Test 页截图确认），Public Client ID = t9KmvYkBSVqr2uAQN12MWQ，**同一 app** ✅

**D. 仍未满足项**
- 账号级 auto-start 列表为空：因为 app 只装在**用户级**。官方："auto-start 列表只显示与当前层级一致的已安装 app"。账号级需 **App listing page → Install for Others**（或 Manage → Apps on account → Users & Groups → +Add Users）。
- **点 "Add for Others" 报 `Redirect URL is required (4700)`** → 官方 quickstart 明确要求：Basic Information → **OAuth Information 里必须填 OAuth Redirect URL**（示例用 ngrok HTTPS）。没填就无法做账号级安装 → 也阻塞"给所有人用"。
- 事件订阅改动后需等 **1–2 分钟**传播（官方反复强调）。

**E. 结论 / 下一步**
1. **验证 RTMS 本体：用 it-services@sding.me 登录桌面客户端 → 本人「发起/主持」一场新会议**（不是点链接加入），auto-start 应自动拉起 RTMS → 推 meeting.rtms_started。
2. 后台已起 webhook 监听器（/tmp/rtms-watch.mjs → /tmp/rtms-watch.log），事件一到即可取证。
3. 修 Redirect URL 后才能做账号级安装（SaaS 化的前提）。

## 18:05-18:15 端到端测试准备
- 用户在客户端以 it-services@sding.me 发起即时会议 88405483460（type 1），仍**无 rtms_started**（等 110s 无事件）→ 说明 host 在场 + app 已装 + auto-start On 仍未触发 RTMS，指向 Zoom 后端未 provision。
- **后端代码关键约束（rtms.service.ts:139）**：只有在 round 的 `meetingLink` 含 `/j/{numericId}` 时才对 meeting.started 自动发起 RTMS 启动；且要求 `transcriptStatus === 'idle'`。`POST /api/meetings/host/link` **只返回链接，不写库**，所以手工调它建的会议不会被自动挂上 RTMS。测试时必须手动 UPDATE round 的 meetingLink。
- requestStream 发参：`{ action:'start', settings:{ participant_user_id: host_id, client_id: ZOOM_OAUTH_PUBLIC_CLIENT_ID } }`（line 49-53，client_id 用 public client，已确认正确）。
- 后端已由我重启到 /tmp/hireos-api.log（PID 92560），日志可见性恢复；token 存于磁盘，重启不需重新授权。
- 新建测试会议 **81650254270**，round dd83db72 的 meetingLink 已手动指向它。

## 18:21-18:30 端到端链路打通 + 锁定 invitee 条件（最后一个未消除变量）
### 后端首次完整跑通自动链路
- 18:21:05 后端收到 meeting.started（81650254270, uuid `xu+n/YTRRUmTcB+ag64Eaw==`, host it-services@sding.me）→ 18:21:06 自动发起 RTMS，`RTMS start accepted ... awaiting meeting.rtms_started` → 但 90s 内 ngrok **零新事件**（第 4 次 204 + 零事件）。
- 结论：webhook 链路、round 匹配、自动启动代码均正常，卡点 100% 在 Zoom 侧是否真正建流。

### 官方文档确认（developers.zoom.us/docs/rtms/meetings/add-features 原文）
- Prerequisites 只有一条：**账号须有 Zoom Developer Pack credits**（已有）。
- **RTMS app 必须是 user-managed**（是）。
- 需要 **Granular scopes**（旧 app 可能未启用，若 scope 列表搜不到 RTMS 即为未启用）。
- REST 启动原文硬条件：**participant 必须是 meeting invitee**（"added to the meeting's invite list, **not** a participant who joined with a shared meeting link"）+ 已入会 + host/alternate host 在场。
- Manuel start 的 REST API 需 scope `meeting:update:participant_rtms_app_status`。

### 关键操作发现：Zoom 建会 API 支持 invitee 字段
- `POST /v2/users/me/meetings` body: `settings.meeting_invitees: [{ email: '...' }]` → **201 成功**。此前所有测试会议 invitees 均为空，所以 REST PATCH 注定被异步静默拒绝（Error 2308）。
- 新建带 invitee 会议 **82960853383**（invitee = it-services@sding.me，即 host 本人），round dd83db72 的 meetingLink 已指向它，transcriptStatus 重置 idle。
- Token 获取工具：`/tmp/tok.mjs`（读取 .local/zoom 加密存储 + PKCE refresh，client_id 用 public client，无需 secret）。

### 待验证
- 用户以 **it-services@sding.me** 身份加入 82960853383 → 后端自动 PATCH → 观察是否首次出现 meeting.rtms_started。

## 18:30-18:40 第 5 次失败：invitee 条件补齐后仍无 rtms_started（定性）
- 会议 **82960853383**（会议 invitees=[it-services@sding.me]，host 本人为 invitee）→ 18:30:12 meeting.started → 18:30:13 后端自动 PATCH → 204 accepted → **90s 内 ngrok 零新事件**，DB transcriptStatus 仍 idle。
- Zoom 无 RTMS 设置类只读 API：`/users/me/rtms/settings`、`/accounts/{id}/rtms/settings` 均 **404 not recognized**；`/accounts/{id}/settings?custom_query=rtms` 需 `account:read:settings:admin`（4711）。→ RTMS 开关不在 API 层暴露，无法自证。
- /users/me 复核：id `613-OuW0RMaHLJfHLFykzg`，email it-services@sding.me，**type=2 Licensed**，Owner。

### 用户侧可控变量已穷尽（逐项为「满足」）
1. user-managed General app ✅
2. Developer Pack credits ✅（Free Trial 0/20）
3. Granular scopes + RTMS scopes（audio/transcript/rtms_started/rtms_stopped）✅
4. RTMS 事件订阅在同一订阅内 ✅（meeting.started 每次准时到达，证明 webhook 链路无问题）
5. app 安装给 host 用户（Local Test → Add app now）✅
6. User Settings → Auto-start = HireOS On；账号/用户级 Share realtime content On ✅
7. host 本人在会中 ✅
8. invitee 条件（会议邀请名单含 host）✅ ← 本轮补齐，仍失败
9. REST PATCH 返回 204 ✅（= 异步受理，非成功）

### 定性结论
**Zoom 后端未给 account_id `M5uc0junTyaMaYqJqj-zKA` provision RTMS entitlement（concurrency 可能为 0）**。与 devforum 同类案例一致：账单显示 Developer Pack 已激活，但后端未开通，需 Zoom 侧手动 provision。
→ 下一步是提交 Zoom 支持工单，同时评估非 RTMS 备选链路。

## 18:41 转向：放弃 RTMS 实时流，改走「会后取转写」（Zoom 自己转写可用）
- 用户反馈：**桌面端能打开实时字幕/转写，会议结束后也有 AI 摘要** → 说明该付费账号的 AI Companion 转写通路本身可用，只是 RTMS 实时管道未被 provision。
- 探测三条替代接口，Zoom 返回精确的缺 scope 清单（4711）：
  - `GET /v2/meetings/{id}/transcript` → 需 **`cloud_recording:read:meeting_transcript`**
  - `GET /v2/meetings/{id}/meeting_summary` → 需 **`meeting:read:summary`**
  - `GET /v2/meetings/{id}/recordings` → 需 **`cloud_recording:read:list_recording_files`**
- 这 3 个均为普通 OAuth user-managed app 可添加（不同于 user:read:user:admin），加后**必须重新授权**（scope 在授权时固化）。
- 路线调整：短期交付走「会后 pull 转写/摘要」，RTMS 定位改为「Zoom 侧 entitlement 待开通」的后续增强（保留自动启动代码 + webhook 处理，一旦授权即可用）。
- 注意：之前连 had 9 个 scope，但不含上述三项，也不含 `meeting:read:list_meetings`。

## 18:52 重新授权完成 → 转写/录制 scope 已生效
- token 于 18:52 更新（pending:false）。三个新 scope 生效，错误从 4711 变为业务层：
  - `/meetings/{id}/recordings` → **404 / 3301** 「此录制文件不存在」
  - `/meetings/{id}/transcript` → **404 / 3322** 「此会议转写文字不存在」
  - `/meetings/{id}/meeting_summary` → **400 / 300** Invalid meeting id（该接口要传 meeting **UUID** 而非会议号，可能需 double-encode）
- **含义：权限通路已通，缺的是「产出」**——桌面端看到的实时字幕不会自动落盘为 transcript 文件。
- 下一步必须在 Zoom 门户开启持久化转录产出：
  1. Settings → **AI Companion** → Meeting Summary → 勾选 **Audio transcript**（并设置 language）
  2. Settings → **Recording** → Cloud recording = On（audio transcript 依赖云录制承载）
  3. 新开一场会 → 讲话 → 结束 → 等 2–5 分钟 → 再拉 `/transcript`

## 19:00 用户需求确认：要的是「实时转录」，暂停 Zoom 联调
- 用户明确指出会后取转录不是他想要的，核心需求是**面试进行中实时出字**。
- **关键限制（官方 Meeting SDK 文档原文）**："Access to raw data — Direct access to raw video and audio data **on native platforms**" → Web 版 Meeting SDK **无法**获取原始音频，只有 native（Win/macOS/iOS/Android）可以。当前项目 `public/zoom-meeting/meeting.js` 是浏览器版 Embedded SDK，故前端抓不到音频。
- 可选实时路径（待用户决策）：
  a) **标签页音频捕获 + Whisper**：`getDisplayMedia({audio:true})` 勾选「共享标签页音频」→ 切片送 Whisper。Chrome only，零 Zoom 依赖，最快见效
  b) 提 Zoom 工单等 RTMS 开通（代码链路已就绪，时间不可控）
  c) Meeting SDK native 桌面端 raw data（可控但工作量最大）
  d) Video SDK 自建房间（最干净但产品形态改变）
- RTMS 现有代码不动，保持可随时启用。

## 19:10-19:15 DecisionPage Regenerate 报 AI_REQUEST_FAILED → 根因是 API Key 失效
- **根因（已严格验证）**：`.env` 的 `HIREOS_AI_API_KEY`（`sk-svcac…mx4A`，len=167，无引号、无截断）被 OpenAI 判定 **401 `invalid_api_key`**。OpenAI 原文：`Incorrect API key provided`。
  - 注意区分：模型不存在会返回 **404 model_not_found**，401 只可能是 key 问题，与模型无关。
- 该 AiService 是所有 AI 生成的唯一出口，所以 Rubric 生成、Brief 出题、Decision 草案全部失败（前端统一显示为 AI_REQUEST_FAILED）。
- **模型已更换**：`HIREOS_AI_MODEL=gpt-5.6-luna`（GPT-5.6 系列低价高速款，对应原 gpt-5-mini 定位；gpt-5-mini 已下架）。
- **代码改进**（`src/intake/ai.service.ts`）：`response.ok === false` 时读取 provider 错误体截断 500 字符并 `Logger.warn`，401/403 细化为新错误码 `AI_AUTH_FAILED`（不可重试），429 仍为 AI_RATE_LIMITED；前端 `features/project-intake/i18n.ts` 已加 AI_AUTH_FAILED 中英文案。→ 以后同类问题在后端日志里一眼可见。
- **待用户**：去 platform.openai.com 生成**新的 project/service key** 替换 `HIREOS_AI_API_KEY`，重启后端后即可恢复（env 在启动时加载，改完必须重启）。
