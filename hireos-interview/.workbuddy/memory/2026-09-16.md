# 2026-09-16 工作日志

## Zoom RTMS 实时转录接口返回 idle 的排查

### 现象
`GET /api/rounds/:id/transcript` 一直返回 `{"status":"idle","error":null,"lines":[]}`。

### 排查结论
链路本身完全正常，问题出在 **Zoom App Marketplace 事件订阅未配置**（`meeting.rtms_started` 事件没订阅 / endpoint URL 没填），导致 Zoom 从未回调后端 webhook。

### 关键事实（已逐项验证）
- 后端代码/配置全部正确：
  - webhook 端点 `POST /api/meetings/webhooks/zoom`（`main.ts` 直接挂在 Express 实例，绕过 WorkspaceGuard）
  - `ZM_RTMS_WEBHOOK_SECRET` 已配置（22 字符）
  - `ZM_RTMS_CLIENT`/`ZM_RTMS_SECRET` 已配置
- round `b48aaedf-...` 的 meetingLink = `https://us05web.zoom.us/j/82083484374?pwd=...`，符合 `/j/${meetingId}` 匹配规则
- ngrok 隧道已运行：`https://retaliatingly-chocolaty-raeann.ngrok-free.dev` → `localhost:3001`
- 通过公网 URL 测试 webhook 端点，`endpoint.url_validation` 返回正确，HTTP 200

### 模拟验证（已完成）
用 Node 脚本按后端签名逻辑（`v0=` + HMAC-SHA256(secret, `v0:${ts}:${JSON.stringify(body)}`)）构造 `meeting.rtms_started` 事件，发送后 round 状态成功 `idle → live`，证明后端处理链路 100% 正常。验证后已把状态恢复为 `idle`。

### Zoom 端待办（用户需手动完成）
1. Zoom App 开启 RTMS feature
2. Webhook Secret Token 设为 `ZM_RTMS_WEBHOOK_SECRET`
3. Event Subscriptions 添加 `meeting.rtms_started` / `meeting.rtms_stopped` / `endpoint.url_validation`，endpoint URL 填 ngrok 公网地址
4. 激活应用

### 关键命令速查
- 后端数据库：`psql "postgresql://hireos@127.0.0.1:55432/hireos_interview"`（无密码）
- 查 round 状态：`SELECT id, "meetingLink", "transcriptStatus" FROM "InterviewRound";`
- webhook 签名验证逻辑在 `src/meetings/rtms.service.ts:23-34`

### 潜在隐患 → 已修复（meeting_uuid 兼容匹配）
确认 `@zoom/rtms` 的 `JoinParams`（rtms.d.ts:408-431）只接受 `meeting_uuid`/`rtms_stream_id`/`server_urls`，**没有数字会议号字段**——Zoom 真实 `rtms_started` payload 只带 uuid，旧代码拿数字号匹配 `/j/${meetingId}` 必然失败。

修复（`src/meetings/rtms.service.ts`）：
- 新增 `uuidToMeetingId` 内存 Map：标准 `meeting.*` 事件（meeting.started 等）的 `payload.object.{id,uuid}` 同时含数字号+uuid，用它建映射；`meeting.ended` 时删除
- `rtms_started` 时：先试 payload 上的数字号（防御），否则用 uuid 查映射；匹配不到 warn（含 uuid）
- 兼容字段嵌套：`payload` 直接含 RTMS 字段或 `payload.object` 嵌套均支持，`client.join(source)` 传实际含字段的对象
- **用户需在 Zoom 事件订阅中额外添加 `meeting.started` 事件**（新逻辑依赖它建映射）

### 额外发现并修复：watch 进程 Prisma Client 类型过期
后端 `nest start --watch` 进程（旧 PID 49946）启动早于 09-16 11:44 的 `prisma generate`，进程内类型缓存过期（报 36 个 TS 错误：transcriptStatus/interviewQuestion/rubricVersion/scheduledAt "不存在"），且 tsc watch 增量编译感知不到 node_modules 变化，编译失败时不重启 app → 一直跑旧代码。
修复：`prisma generate` 重跑 + kill 旧进程 + `npm run start:dev` 后台重启（新 PID 60334，0 编译错误）。
教训：改后端代码后若 watch 日志显示编译错误且错误数不变，先怀疑进程内 Prisma Client 缓存过期，直接重启进程。

### Zoom 配置答疑（用户截图反馈）
- `endpoint.url_validation` 在事件列表搜不到是正常的：它不是可订阅事件，是保存 endpoint URL 时 Zoom 自动发的验证握手（main.ts 已处理，已 curl 验证可正确响应）
- **RTMS 账户开关位置（已更正）**：不在个人 Settings → Meeting，而在 **`https://zoom.us/account/setting` → 「Zoom Apps」标签页 → "Share realtime meeting content with apps"**（官方文档 developers.zoom.us/docs/rtms/meetings/ux-host-admin-tools-ctrls/）
- RTMS 所需 scopes（Marketplace → 应用 → Scopes）：`rtms:read:rtms_started`、`rtms:read:rtms_stopped`、`meeting:read:meeting_transcript`、`meeting:read:meeting_audio`。加完 scopes 后事件列表才会出现 RTMS 分类

### Marketplace 事件显示名 ↔ event 名映射（易混淆，实测确认）
| Marketplace 显示名 | event 字段 |
|---|---|
| **Start Meeting** | `meeting.started` |
| End Meeting | `meeting.ended` |
| RTMS Started in Meeting | `meeting.rtms_started` |
| RTMS Stopped in Meeting | `meeting.rtms_stopped` |
| Participant joined meeting before host | `meeting.participant_joined_before_host`（非通用） |

搜索框搜 "started" 会命中 Sharing/Live Streaming "Started" 等子串，直接搜 "Start Meeting" 或在 Meeting 分类下滚动更快。

### RTMS 官方硬前提与 scope 细节（查官方文档确认）
- **RTMS 需要付费：Zoom Developer Pack credits**（官方原文 "To use RTMS, you'll need Zoom Developer Pack credits on your account"，定价 https://zoom.us/pricing/developer）。没有 credits 即使配置全对也不会推数据 —— 排查时的首要怀疑项。
- RTMS app 必须是 **User-managed app**；需要 **Granular scopes**（能看到 RTMS scope 分类即已启用）
- Scopes 列表里那个灰色复选框是 **「Optional」列**，不是"选择/添加"框。官方明确 "RTMS scopes are unable to be marked as optional"，所以 `meeting:read:meeting_audio` / `meeting:read:meeting_transcript` 灰框属正常，出现在列表里即已添加成功
- 必需 scopes：`rtms:read:rtms_started`、`rtms:read:rtms_stopped`、`meeting:read:meeting_audio`、`meeting:read:meeting_transcript`（可选 `meeting:update:participant_rtms_app_status` 走 REST API 控制）
- 官方提示：保存事件订阅后需等 1-2 分钟生效再测试

### RTMS 计费与开通（官方论坛/定价页确认）
- 计费：按分钟，纯流不含转写 **$0.01/会议流分钟**；购买入口 https://zoom.us/pricing/developer
- 方案：Pay as You Go（按量后付费）/ 100 credits / 500 credits / 500+ 联系销售
- **有 "Try 20 Credits Free" 免费试用额度**（够验证链路）
- 历史：2025-09 前 RTMS 需向 Zoom 申请 enablement；2026-05 起支持自助购买
- 常见坑：论坛大量 "error 13262: not authorized to access meeting content despite completing all UI setup steps" —— UI 配好但账号无 RTMS 授权/credits，与本次现象吻合
- 查看订阅状态：Zoom 账号 → 「计划管理」标签页（非「计费管理」，后者只显示付款方式/联系人）

### RTMS 启动方式（关键，极易漏）
RTMS 流**不会**随会议自动开始，必须通过以下方式之一启动，`meeting.rtms_started` 事件才可能发出：
1. **Auto-start（本项目采用）**：zoom.us/account/setting → Zoom Apps 标签页 → 先开 "Share realtime meeting content with apps" → 再点该区下方的 **"+ Choose an app to auto-start"** 选中 app
   - ⚠️ **UI 文案已改名**：官方论坛旧文案是 "Auto-start apps that access shared realtime meeting content"，现版 UI（2026-09 实测）该区块标题为 **"Apps that access content in meetings"**，副标题 "Manage how apps access meeting content for users on this account."，入口是链接 **"+ Choose an app to auto-start"**。搜这两个词都找不到，别再按旧文案找。
   - 同一页 General 区的 "Auto-open an app in meetings" **是另一回事**（自动打开 app 界面），别混淆
   - 该列表即 API 报错 13262 所指的 "Allow apps to access meeting content" 白名单 —— auto-start 与 REST API 两条路**都要求 app 在此列表内**
   - 该区提示 "Only apps already added for all users can be selected. To add one, find it on marketplace.zoom.us and select 'Add for Others'." → 本地测试态的 user-managed app 可能选不到，这是常见卡点

#### ⚠️ 实测卡点：auto-start 名单里找不到自己的 app（2026-09-16 用户实际遇到）
**官方开发者论坛原话**：Zoom 有**两个**不同的 Zoom Apps 设置页
- **User Settings → Zoom Apps** = 装给「个人」的 app
- **Account Settings → Zoom Apps** = 装给「整个账户」的 app
- **"The auto-start list only displays apps that are installed for the same level (user, group, or account) that you are viewing."**
- "Even if you are the only user, Zoom still differentiates between 'installed for me' and 'installed for the account'."

→ 本案 app 只装给了个人（Local Test → Add app now），而用户看的是**账户级**设置页，所以名单为空。
**两条解法**：
1. **装给全账户**：Marketplace → Develop → 应用 → Open App Listing Page → **Install for Others**（或 `https://marketplace.zoom.us/user/installed` → 点应用 → Add for Others）→ 回账户设置即可见
2. **改用个人级设置页**：`https://zoom.us/profile/setting?tab=zoomapps`，在个人 Zoom Apps 里配 auto-start（官方 quickstart 走的就是这条路："Open your Zoom settings → Choose the Zoom Apps tab"）
- 参考：devforum.zoom.us/t/rtms-account-access-how-to-get-started/137072/131
- 同帖另一案例：Event/scope/开关全对但名单只有 "None" → 缺 Developer Pack 或 app 未分配用户
- **进展（2026-09-16 17:08）**：用户在**个人级** `https://zoom.us/profile/setting#tab-zoomapps` 成功把 app 加进了 auto-start 名单（走的是解法 A）
- **测试要点**：必须用「安装并授权了 app 的那个 Zoom 账号」当**主持人**、并且**主持人本人入会**；开会时保持 Host tools → Share realtime meeting content with apps 开启
- 两个 round 的会议：Round1 `82083484374`（9/20）、Round2 `88684411816`（9/22），均可提前由主持人启动

### 🔑 突破性诊断：项目里存在两个不同的 Zoom App（2026-09-16 17:10）
用 ngrok inspector（`http://127.0.0.1:4040/api/requests/http?limit=30`）查隧道内所有请求，**这是判断"Zoom 到底有没有回调"的权威手段**：
```
14:10:55 POST /api/meetings/webhooks/zoom  UA=Zoom Marketplace/1.0a  200   ← 订阅保存握手
14:15:48 POST /api/meetings/webhooks/zoom  UA=Zoom Marketplace/1.0a  200   ← 再次验证
（此后 Zoom 零请求，用户开会期间也无）
```
→ **事件订阅已保存成功、端点已验证通过，但 Zoom 一条事件都不推。**

**根因线索：两个 Marketplace app 混用**
| .env key | 值（脱敏） | 用途 | 被谁引用 |
|---|---|---|---|
| `ZM_RTMS_CLIENT` / `ZM_RTMS_SECRET` | `o_OQnk4F...` / `o3XriQaR...` | RTMS（@zoom/rtms SDK 直接读 env） | src 中**无引用**（SDK 自行读 process.env）|
| `ZOOM_MEETING_SDK_CLIENT_ID` / `_SECRET` | `o_OQnk4F...`（同一个 app） | Meeting SDK JWT | `meetings.service.ts:14,34` |
| `ZOOM_OAUTH_PUBLIC_CLIENT_ID` | **`SNF7ysbn...`（另一个 app）** | OAuth 连接 Zoom 账号 / 建会议 | `zoom-host.service.ts:43,103` |

- 加密存储（`.local/zoom/<ownerHash>`，aes-256-gcm，AAD=owner）解出：已授权账号 = **苏俊杰**（zoom userId `6H_dziZYRL2GSu3zNo47og`），**其 clientId = `SNF7ys...`**（即授权的是 OAuth app，不是 RTMS app）
  - meetings 映射：`default -> 81822123493`、`b48aaedf-8347-4cbc-b123-b4b66c9f0f78 -> 82083484374`
- user-managed app 的 webhook 事件**只对"已授权该 app 的用户"推送** → 订阅所在 app（RTMS app）若没被 苏俊杰 授权，Zoom 永远不推
- `ZM_RTMS_WEBHOOK_SECRET` = `y_YSDJZ2...`，url_validation 能通过说明订阅所在 app 的 Secret Token 就是这个值

**待用户确认**：① auto-start 里加的是哪个 app ② `https://marketplace.zoom.us/user/installed` 已装哪些 app ③ 当前会议号

**用户答复（17:15）**：
- app 名称 = **HireOS Interview**；已安装列表（`marketplace.zoom.us/user/installed`）只有 **`HireOS Interview (Development)`**（另有 Otter.ai / AI Chat 等无关 app）
- **用户测试时开的会议 = `81822123493`** = 后端加密存储里的 `default` 会议，**不是任何 round 的会议** → 即使事件到达也会 `no round matched`
- Round 1 正确会议：`https://us05web.zoom.us/j/82083484374?pwd=Ehh63msbbBERlXLAaWaz5wZawKQaB9.1`
- 待用户核对：Marketplace → HireOS Interview → **App Credentials 的 Client ID** 是 `SNF7ys...` 还是 `o_OQnk4F...`（决定 .env 里哪个变量填错）
- 另一待办：user-managed app **新增 scope 后必须重新授权**（重新走 Local Test → Add app now / Authorization URL）

### ✅ 已执行修复：方案 A —— 统一成一个 Zoom app（2026-09-16 17:22）
**改动**：`.env` 的 `ZOOM_OAUTH_PUBLIC_CLIENT_ID` 由 `SNF7ysbnQ3ux7bGFvrERmA` 改为 **`o_OQnk4FQLC7V8vpmsWnA`**（与 `ZOOM_MEETING_SDK_CLIENT_ID` / `ZM_RTMS_CLIENT` 统一）
- 备份：`.env.bak-20260916-172154`
- 重启后端（新 PID 64657），`GET /api/meetings/host/status` → `{"connected":false,...}`（旧授权绑定 SNF7ys，需重新授权）✅ 符合预期

**关键代码事实（`zoom-host.service.ts`）**
- OAuth 走 **PKCE 公开客户端**：token 交换只用 `client_id` + `code_verifier`（L124/L155），**没有 client secret** → 所以 .env 里没有 `ZOOM_OAUTH_PUBLIC_CLIENT_SECRET`
- 回调地址是**随机端口的本地回环**：`http://127.0.0.1:${server.address().port}/api/integrations/zoom/callback`（L142）→ **新 app 的 OAuth Redirect URL 必须配成与旧 app 一致**，否则授权必失败
- `status()` L103 / `account()` L153 都用 `clientId !== this.clientId()` 判定未连接 → 换 app 后必须重连
- L129：重新授权时若 Zoom `user.id` 相同则**保留原 meetings 映射**（苏俊杰 不变，所以 82083484374 不丢）
- 前端入口：`job-Interview-front/src/features/live-interview/ZoomHostPanel.tsx` 的「连接 Zoom / 重新授权」按钮（调 `/api/meetings/host/authorize`，PKCE popup）

**待用户完成**：① 在 "HireOS Interview" app 配 OAuth Redirect URL（照抄 SNF7ys 的）② 确认 app type = General App (User-managed) ③ 前端重新授权 ④ 用 `82083484374` 开会
**回滚**：`cp .env.bak-20260916-172154 .env` + 重启

### 本次踩坑记录
- grep 报错 `(eval):1: no matches found: --include=*.ts` 是 zsh glob 问题 → 用 Grep 工具而非 shell grep
- ngrok inspector 的 `start` 字段是**纳秒字符串**，`int(start)/1e9` 才是秒
2. REST API `PATCH https://api.zoom.us/v2/live_meetings/{meetingId}/rtms_app/status`（需 `meeting:update:participant_rtms_app_status` scope；**本项目后端未实现此调用**）
   - body: `{ action: "start"|"stop"|"pause"|"resume", settings: { participant_user_id, client_id } }`；`client_id` = RTMS app 的 Client ID
   - 需 StoS app 取 token（`POST https://zoom.us/oauth/token?grant_type=account_credentials&account_id=...`），scope 需 admin 版 `meeting:update:participant_rtms_app_status:admin`
   - 2026-01-18 起：**host 不必先入会**也可用 host 的 OAuth token 启动（此前必须先入会）
   - 关键错误码：`13262` app 未加入 "Allow apps to access meeting content" 白名单；`13267` RTMS app disabled（需开 "Share realtime meeting content with apps"）；`2308` 仅 host/联席主持人可操作；`3000` 会议未开始
3. Zoom Apps SDK `startRTMS()`（需做成 in-meeting 插件）
- user-managed app 还需用户本人通过 Marketplace → 应用 → Local Test → Authorization URL 授权安装
- 结论：本项目链路要通，必须配好账户级 Zoom Apps 开关 + 把 app 加进 "Choose an app to auto-start" 名单

---

## ⚠️ 更正：不存在"两个 app"——SNF7ys 是同一个 app 的 Public Client ID

**用户截图证据**（Marketplace → HireOS Interview → App Credentials）：
- **Client ID** = `o_OQnk4FQLC7V8vpmsWnA`
- **Public Client ID** = `SNF7ysbnQ3ux7bGFvrERmA`（"Use Public Client OAuth" 开关为 ON）
- OAuth Redirect URL（Development）= `http://127.0.0.1/api/integrations/zoom/callback`（loopback PKCE，仅校验 host 不校验端口）

→ Zoom General App 有**两个 client id**：常规 `Client ID`（配 secret，服务端用）与 `Public Client ID`（PKCE 公开客户端用）。
- **PKCE 公开客户端流必须用 Public Client ID** → `ZOOM_OAUTH_PUBLIC_CLIENT_ID=SNF7ysbnQ3ux7bGFvrERmA` **原本就是对的**
- 上一轮误判为"另一个 app"并改成 `o_OQnk4F...`，导致 `status()`（`zoom-host.service.ts:103` 比对 `account.clientId === ZOOM_OAUTH_PUBLIC_CLIENT_ID`）返回 `connected:false`
- **已回滚**为 `SNF7ysbnQ3ux7bGFvrERmA`，重启后 `GET /api/meetings/host/status` → `{"connected":true,"name":"苏俊杰"}` ✅
- `.env.bak-20260916-172154` 是**错误版本**的备份，不要用它回滚

**定论**：`.env` 三处 client 配置本就一致且正确（Client ID 给 RTMS/Meeting SDK；Public Client ID 给 OAuth PKCE）。
`idle` 的根因不在 app 配置。

### 新增埋点：webhook 入口持久化日志（`src/main.ts`）
每次到达的 Zoom 请求都会写 `[ZoomWebhook] received event=<event> ua=<UA>` 到 `.local/api.log`
（ngrok inspector 记录会滚动、SDK 自带 logger 默认关闭，这条日志让"Zoom 没推" vs "推了但没处理好"可区分）。已验证生效。

### ngrok 隧道请求全量记录（本日）
| 时间(本地) | 来源 | 结果 |
|---|---|---|
| 12:05:11 | curl（我方探针） | 200 |
| **14:10:55** | **Zoom Marketplace/1.0a** | 200 ← 保存订阅的 url_validation 握手 |
| **14:15:48** | **Zoom Marketplace/1.0a** | 200 ← 第二次（又保存了一次） |
| 16:12 / 17:10 | curl（我方探针） | 200 |

→ **14:15:48 之后 Zoom 一条都没推**（用户实际开会期间也是零）。
若用户此后从未再点 Save，则后加的 `meeting.started` / RTMS 事件可能**未被保存**（每次 Save 都会触发新的 url_validation，而记录里没有第三条）。

### 环境运维
- 多轮后台启动残留了多个 `nest start` 进程互抢 3001（`EADDRINUSE`）。清理：`pkill -f "nest start"; pkill -f "npm run start:dev"` 后按端口 kill，再只启一个
- 前端 `job-Interview-front` 跑在 **5173**，`vite.config.ts` 有 `proxy: { '/api': 'http://127.0.0.1:3001' }` → 同源，不受 CORS 影响（`.env` 的 `CORS_ORIGIN=http://localhost:5174` 是过期值，无害）

---

## 疑点收敛（用户截图 + 官方文档核实）

**已排除**
- ❌ 多 app 混淆：`Develop → Created apps` 里**只有一个 app**「HireOS Interview」（另有 Otter.ai / AI Chat 是已安装的第三方 app，不是自建）
- ❌ 账号错位：用户开会登录的账号就是 **苏俊杰**，与后端加密存储的授权账号一致
- ❌ Draft 状态：Zoom 开发者论坛有专帖《Does a Draft (Development) user-managed app receive webhook events at all?》→ **答案：会收到**，Draft (Development) user-managed app 正常收事件。故 `Draft` **不是**原因
- ❌ 签名密钥不匹配：14:10/14:15 两次 url_validation 都成功（响应被 Zoom 接受、订阅才存得下），说明 `ZM_RTMS_WEBHOOK_SECRET` == app 的 Secret Token

**官方文档关键事实（developers.zoom.us/docs/api/webhooks/）**
- URL 验证（CRC）与「订阅哪些事件」是**两个独立步骤**：验证只需 endpoint URL；事件要单独 `+ Add Events` → **Done** → 再 **Save**。→ **一个"端点已验证、但事件一个都没订阅"的订阅，症状恰好就是"验证通过 + 零事件投递"**
- "Zoom requires that you **manually** trigger webhook validation... Subsequently Zoom automatically revalidates **every 72 hours**"
- "You won't be able to save your changes until after Zoom validates your endpoint"
- 连续 6 次重验证失败 → Zoom **停止投递并禁用订阅**
- Marketplace UI 建订阅时还有 **Event notification receiver** 选项：`All users in your account` / `All users in master account and subaccounts` / `Only for users who have added this app`

**⚠️ 我犯的错（已更正）**
- 我让用户去点 endpoint URL 旁的 **`Validate` 按钮** —— 论坛明确说：**Development 环境没有 Validate 按钮**（只有 Server-to-Server OAuth app 有）。该指令无效

**当前首要假设**
事件订阅里**根本没有保存任何事件**（只验证了 URL）。这与"端点验证过 + 零事件投递"完全吻合。
次要假设：endpoint URL 后来被改成了**旧的 ngrok 地址**（重新验证过才能保存，但 ngrok inspector 记录里没有第三条验证记录）。
→ 需要用户提供 Event Subscriptions 区域截图（endpoint URL + 事件列表 + Save 按钮状态）确认。

**ngrok inspector 查询方式**：`curl -s http://127.0.0.1:4040/api/requests/http?limit=50`，`start` 字段是**纳秒字符串**，`int(start)/1e9` 才是秒。
请求体取法：`request.raw` 是**整段 HTTP 报文的 base64**（`body` 字段可能为空），解码后按 `\r\n\r\n` 切出 body。

### ✅ 2026-09-16 17:51 突破：真实 Zoom 事件首次到达
**根因确认**：事件订阅里确实**一个事件都没保存**（只验证了 URL）。用户在 Event Subscriptions 勾选 Start Meeting + RTMS started in Meeting + RTMS stopped in Meeting 后点 **Save**，Zoom 立刻推事件 → 假设成立。

**收到的第一个真实事件**（17:51:51，UA `Zoom Marketplace/1.0a`，HTTP 200）：
```json
{"event":"meeting.started","payload":{"account_id":"5Y_VtKA3S0KcgWYa3hfBHw",
 "object":{"topic":"HireOS Interview — Round 1","id":"82083484374",
 "uuid":"cH5gzBDLSPeMYFcLiYJP2A==","host_id":"6H_dziZYRL2GSu3zNo47og",
 "host_email":"sujunjie427@gmail.com","type":2,"duration":60}}}
```
- 会议号 `82083484374` = Round 1 的会议 ✅
- `host_id` = 苏俊杰（与后端授权账号一致）✅
- 请求头 `Clientid: o_OQnk4FQLC7V8vpmsWnA` ✅
- **`payload.object` 同时含 `id`（数字号）和 `uuid`** → 证明 uuid→数字号 桥接方案正确
- 结论：**`meeting.started` 的 payload 结构已实测确认**，不再依赖猜测

**下一步待验证**：`meeting.rtms_started` 是否到达。需 auto-start 状态为 On。

**⚠️ 编辑 `src/**` 会触发 `nest start --watch` 自动重启 → 内存里的 `uuidToMeetingId` 映射会丢失**。会议进行中不要改代码，否则 rtms_started 会匹配不到 round。

### 🎯 2026-09-16 18:00 真正的根因：error 13262（已用 API 实测证明）
用后端加密存储的 token 直接调 Zoom API：`PATCH https://api.zoom.us/v2/live_meetings/{meetingId}/rtms_app/status`，body `{action:"start",settings:{participant_user_id:<host_id>,client_id:"o_OQnk4FQLC7V8vpmsWnA"}}`
- 对**正在进行的** `81822123493` → **HTTP 403 `{"code":13262}`**："The app 'o_OQnk4FQLC7V8vpmsWnA' is **not authorized to access meeting content**. Please add it in the **Allow apps to access meeting content** setting in the **Settings** page of the Zoom web portal."
- 对未开始的 `82083484374` → HTTP 400 `{"code":3000}` "has not started"（证明 token/scope/调用方式都对，只差白名单）
→ **RTMS 永不 HTTP 自动启动的根因就是 app 不在账户级白名单里**。这也是 `meeting.rtms_started` 永远不来的原因。

**官方解法（Zoom 员工 chunsiong.zoom 在 devforum 142847 / 144973 帖的回复）**
- 去 **`https://zoom.us/account/settings`**（**账户级**，非 `/profile/setting` 个人级）→ **Zoom Apps** 标签页
- 打开 **"Share realtime meeting content with apps"**
- 在 **"Apps that access content in meetings"** / **"Allow apps to access meeting content"** 处点 **Edit** → 找到 "HireOS Interview" → 把 **Auto-start 状态设为 On**
  - 这一步**本身就是把 app 加进白名单**，同时实现自动启动（一举两得）
- 若列表里选不到该 app：账户级列表只显示"已为所有用户添加"的 app → 去 marketplace.zoom.us 的 app listing 页选 **"Add for Others"**

**错误码对照（来自 Zoom API 文档）**
- `300`无效meetingId / `3000`会议未开始 / `3001`会议不存在
- `2308` 非主持人或联席主持人 / `2310` RTMS 操作被拒（**无 Developer Pack / Basic 账号**）
- `13262` app 未加入 "Allow apps to access meeting content" 白名单
- `13267` RTMS 被禁用（需开 "Share realtime meeting content with apps"）
- `13273` 当前会议不支持 RTMS / `13277` 该 app 尚未启动

**⚠️ 仍未排除的隐患：账号是 Basic 免费版**
`GET /v2/users/me` → **`type: 1`** = **Basic（免费）**，账号 `sujunjie427@gmail.com`，role Owner。
Zoom 官方 troubleshooting「Plan compatibility」表：**Basic (Free) → Not supported → RTMS webhooks will not fire; API calls will fail with error 2310**。
我们目前收到的是 13262（白名单）而非 2310，所以**尚未证伪**；若修好白名单后出现 **2310**，就是 Basic 账号不支持，需升级付费方案。

### 🛠 2026-09-16 18:01 新增：后端自动启动 RTMS（解决"要自动、不要手点"）
`zoom-host.service.ts`：新增 `anyAccessToken()` —— 遍历 `.local/zoom/` 取任一已连接账号的有效 token（RTMS webhook 没有身份可 key）。
`rtms.service.ts`：注入 `ZoomHostService`；`meeting.started` 且匹配到 round 且 `transcriptStatus==='idle'` 时调用 `requestStream()` → PATCH rtms_app/status `action:"start"`。用 `startRequested` Set 防重复；`meeting.ended` 时清理。
**实测**：模拟 `meeting.started`(82083484374) → 后端自动发请求 → 日志 `RTMS start rejected for meeting 82083484374 (400): {"code":3000,...}`（预期）；模拟非 round 的 `81822123493` → 完全不触发 ✅
→ 白名单修好后，开会即自动启动，返回 204，随后 Zoom 推 `meeting.rtms_started`。

### 🔒 2026-09-16 18:03 最终根因确认（唯一剩余阻塞）
18:02:33 用户又用 Round 1 链接开了真实会议，`meeting.started` 正常到达；但自动启动**被跳过** —— 因为 18:01 我的模拟测试把 `startRequested` 标记占住了（该 Set 原本只在 `meeting.ended` 时清除）。
直接用后端 token 探 API（会议进行中）：`PATCH /v2/live_meetings/82083484374/rtms_app/status` → **HTTP 403 / code 13262**（不是 3000，证明会议确实在开）。
→ **结论：唯一剩余阻塞 = app 未加入账户级白名单**。Zoom 员工口径路径：`https://zoom.us/account/settings` → Zoom Apps → "Apps that access content in meetings" → Edit → 把 HireOS Interview 的 **Auto-start 设为 On**（此动作本身即加入白名单）。

### 🛠 2026-09-16 18:05 两个隐患修复 + uuid 反查
1. `requestStream`：失败后**清除** `startRequested` 标记（原本失败也占位，导致整场会议再无法重试）；对可重试错误（5xx / code 3000）最多重试 3 次、间隔 5s/10s；403 等配置类错误立即停止。
2. 新增 `resolveMeetingId(uuid)`：内存映射丢失（后端重启）时，用 `GET /v2/meetings/{double-encoded uuid}` 反查数字会议号并写回映射。
**实测**（重启后端使内存映射为空 → 只发 uuid-only 的 `rtms_started`）：`uuid lookup` 成功 → 匹配 Round 1 → 状态 `live` ✅（旧逻辑此时会 `no round matched`）

### 💳 2026-09-16 18:07 Developer Pack 计费事实（澄清"Basic 不支持"的疑问）
用户账号「计划和计费 → 计划管理」显示：**Zoom Developer Pack | Free Trial，0 of 20 credits (0% used)**。
- Developer Pack 的产品定位就是 **"Unlock Zoom's API power with versatile access to QSS and RTMS"** —— RTMS 正是这 20 credits 解锁的能力，因此它满足官方 "To use RTMS, you'll need credits on your account" 的前提。
- **0% 使用率是佐证而非故障**：credits 只在 RTMS 流真正运行时才扣费；流从未启动 → 0 消耗。
- 未用额度 **12 个月过期**（试用 2026-08-29 起算）。
- RTMS 单价（定价页）：**RTMS Stream €0.0092/分钟**、**RTMS Transcription €0.0184/分钟**。定价页同时标注 "Available on paid plan only"，含义含糊（官方论坛 146400 帖刚提出此疑问、尚无回复）；官方文档只写"需要 credits"。
- ⚠️ **修正此前判断**：不应急着认定 "Basic(type=1) 不支持 RTMS / 会报 2310"。拥有 Developer Pack credits 后该结论不成立的可能性较大；只有修好白名单后仍报 2310 或 paid-plan 类错误，才需要升级 Zoom Workplace 付费计划。
- 18:07 再次实测（会议仍在进行中）：`PATCH .../82083484374/rtms_app/status` 仍 **403 / 13262** → **白名单仍未修**，这是唯一剩余阻塞。

### 🔍 2026-09-16 18:11 白名单根因：REST 启动 API 的两个硬前提不满足
官方文档（rtms/meetings/add-features，REST API 启动 RTMS 一节）明确：
> **Your app can start RTMS for a participant when BOTH are true:**
> 1. **该 participant 是会议的 invitee**（在会议邀请名单里，而不是凭共享链接入会的人）
> 2. 该 participant **已入会**

而我们的 `PATCH /live_meetings/{id}/rtms_app/status` 传的 `participant_user_id = 6H_dziZYRL2GSu3zNo47og`（苏俊杰）——他是**主持人**，不一定以 invitee 身份出现在该会议的邀请名单里。**13262 "not authorized to access meeting content" 很可能是这个 invitee 前提没满足**，不一定是"Allow apps to access meeting content" 名单问题。

**用户已确认的事实**（务必记住，别再让用户重复）：
- `https://zoom.us/account/settings`（账户级 Zoom Apps）下 **找不到 app**，用户已试过；
- 后来是在 **`https://zoom.us/profile/setting#tab-zoomapps`（个人级）** 里成功添加了 app。

**单用户账户机制**（Zoom KB 0061045）：single-user account（免费或付费）的 Zoom Apps **默认启用、无需预批准**（没有其他用户可批准），所以账户级"Add for Others / pre-approval"流程对单用户账户**不适用/不可见** —— 这解释了为什么 account/settings 下找不到 app。

**待办方向**：
- RTMS 官方推荐 REST 启动需 invitee 身份；要满足需让"苏俊杰"成为该会议的 invitee（通过 Zoom REST 创建会议时把 host 加进 invitees / 或直接改用 auto-start 白名单路径）。
- 尚不能 100% 确定 13262 是 invitee 还是白名单；下一步可选：① 用 Zoom REST `GET /meetings/{id}/invitation` 或 `GET /meetings/{id}` 查该会议 invitees 是否含 host；② 确认个人级 profile/setting 里 app 的 Auto-start 是否真的设成了 On（个人级也许就能触发 auto-start，从而绕开 REST invitee 限制）。

### 🎯 2026-09-16 18:14 真凶找到：Permissions = "Always ask"
用户截图（Apps that access content in meetings 列表，Meeting 区块）：
```
Apps             Auto-start   Permissions
HireOS Interview All          Always ask   ✏️ 🗑
```
- ✅ "Share realtime meeting content with apps" = ON
- ✅ app 在名单里、Auto-start = All（说明 auto-start 本身已开）
- ❌ **Permissions = "Always ask"** ← 这就是 13262 的直接原因：每次访问会议内容都需要 host 在会中批准；而后端 REST 启动时无人批准 → "not authorized to access meeting content"。
- 官方 participant UX 文档对应流程："Request to share content" 弹窗 → host 批准；用户用的是内嵌 Meeting SDK 客户端，批准弹窗被隐藏 → 永远无法批准。
- **修复**：点击行末 ✏️ 铅笔图标 → 把 Permissions 从 "Always ask" 改为 **"Always allow"**（自动放行）→ 保存 → 等 1-2 分钟 → 重开会议。

### 🔬 2026-09-16 18:16 决定性证据：meeting_invitees = []（空）
18:15:52 用户重开会议，后端自动重试 REST 启动仍 **403/13262**。用 `GET /v2/meetings/82083484374` 查会议详情：
- `uuid = ZVC5FNjoTp+jY12eA6japQ==`，`host_id = 6H_dziZYRL2GSu3zNo47og`，`type = 2`（scheduled）
- **`settings.meeting_invitees: []`** ← 空数组。REST 启动 RTMS 的前提是"participant 是会议的 invitee"，主持人苏俊杰不在 invitees 里 → 13262 的**根本原因坐实**（或与 Permissions 双重叠加）。
- 注意：会议 uuid 每次创建都会变（之前 `cH5gzBDLSPeMYFcLiYJP2A==` / `7MYCvE+DQmSZ49l9OJ4Urg==`，现在 `ZVC5FNjo...`），说明 Zoom 是按"每次 Schedule"生成独立会议，不是 PMI 复用。
- **两条落地路径**：① 后端改用 Zoom REST 创建/更新会议时把 host 加进 `invitees`（或 `PATCH /meetings/{id}` 设置 invitees），或 ② 走 auto-start 白名单（Auto-start=All 已开，但 Permissions 需 Always allow），让 Zoom 端自动推 rtms_started、绕开 REST invitee 限制。

### 🚫 2026-09-16 18:19 权威结论（官方 troubleshooting）：Basic 账号硬限制
18:18 用户已把 Permissions 改成 "Always allow"，18:18:49 重探仍 **403/13262**。
**官方 troubleshooting 错误码表（developers.zoom.us/docs/rtms/troubleshooting）决定性信息**：
- **13262 根本不在官方排障错误码表里**（官方列的是 2308/2310/13267/13273 等）。
- **2310 = "Account type does not support RTMS (Basic accounts)"**。
- **Plan compatibility 表**：`Basic (Free) → Not supported`，*"RTMS webhooks will not fire; API calls will fail with error 2310"*；`Pro/Business/Enterprise → Supported`（均需 Developer Pack 订阅）。
- 账号实测 `type=1`（Basic）。收到的错是 13262 而非 2310，说明被 app 授权层（"Allow apps to access meeting content"）先挡下；但根子上 Basic 账号 + 免费试用 Developer Pack 的**组合不满足**官方"付费计划 + active Developer Pack"要求。
- ⚠️ 关键待办：确认用户领的 "Zoom Developer Pack Free Trial（20 credits）" 是否算 "active subscription"。官方定价页原文 "Try 20 Credits Free"；troubleshooting 2310 的解决是 "If using a Basic account, **upgrade to a paid plan** and purchase Developer Pack credits" —— 即 **Basic 账号即使有试用 credits，仍可能不满足 RTMS 的 paid-plan 前提**。
- **结论**：要真正跑通 RTMS，最可能必须先升级 Zoom 到付费计划（Pro 起）再配合 Developer Pack credits；免费试用 credits 在 Basic 账号上大概率无效。

### 🔍 2026-09-16 18:30 补充排查（screenshot 追问 "0/20 credits"）
- **0/20 credits 属正常**：credits 只在流真正运行时消耗；REST 启动从未成功过（一直 13262），流从未跑起来。
- **18:05:04 那条 `meeting.rtms_started` (ua=node) 是上轮调试的本机模拟**，不是 Zoom 真实推送：ngrok inspector 里 18:05 无任何请求（真实事件 UA=Zoom Marketplace/1.0a 且必经 ngrok）；其 uuid `7MYCvE+DQmSZ49l9OJ4Urg==` 取自 18:02:33 真实 meeting.started；SDK `client.join()` 返回真只是本地 native 调用受理，媒体从未流动（71103 无任何到 Zoom 的连接、0 行转写、round 状态最终仍 idle）。**ua=node ≠ Zoom 真事件**。
- **18:28 实测仍 403/13262**（token 有效至 18:51），确认 "Always allow"（会内权限）不解决 REST 启动路径授权。探针脚本：`backend/.local/test-rtms.mjs`（解密 .local/zoom 账号→自动刷新 token→PATCH 重测）。
- **13262 官方修复路径**（developers.zoom.us/docs/rtms/meetings/ux-host-admin-tools-ctrls/）：zoom.us → Admin → Account Management → Account Settings → **Zoom Apps 标签** → ① 开启 **"Share realtime meeting content with apps"** → ② 下方 **"Auto-start apps that access shared realtime meeting content"** → + Choose an app → HireOS Interview → On。**关键：总开关关闭时 app 根本不会出现在可选列表 —— 这就是之前 "account/settings 找不到 app" 的真正原因**（用户当时只加了 profile/setting 的用户级列表）。配好 auto-start 后无需 REST，Zoom 会议开始自动推 rtms_started（= 真正的自动转录路径）。
- **若配置齐仍 13262** → Zoom 后端 entitlement 未生效：devforum 今日(9/16)有完全同配置案例（有 Developer Pack 仍 13262、app 不在列表），MVP 指向 account/settings 开关；4 月案例配置全对仍 13262，结论是需 Zoom 侧 backend entitlement（等传播或联系开发者支持）。客户端需 6.5.5+。
- **僵尸进程**：旧 tsc watch 对 66637/66674 仍存活（cwd=backend，stdout 接 api.log，无 TCP），定期刷 "Found 25 errors" 编译日志污染 api.log；71103 是唯一 3001 监听者。未杀（无法用 ps 确认父子关系，怕误伤 71103 的父链），后续可用 `kill 66637 66674` 清理并观察 3001。

### 🔑 2026-09-16 18:45 "Add app 列表里没有我的 app" 的原因（官方 add-features 文档）
用户已开启账户级 "Share realtime meeting content with apps"，但 "Apps that access content in meetings" 的 + Add app 选择器里没有 HireOS Interview。官方 developers.zoom.us/docs/rtms/meetings/add-features/ 关键事实：
- **app 必须挂着 RTMS 媒体 scope 才算 "RTMS app"**（选择器只列这类 app）。用户之前添加 `meeting:read:meeting_audio/transcript` 时勾选不了（当时截图反馈过）→ app 没有 RTMS scope → 不出现在列表。这就是根因。
- **"Your app will need Granular scopes to use RTMS scopes... Update your app if you do not see RTMS scopes"** —— 老 app 需切到 Granular scopes 模式才能看到/勾选 RTMS scopes；且 **RTMS scopes 不能设为 optional**（强制必选）。
- Developer Pack credits 是唯一前提（用户已满足，9/15 生效）——当时 scope 勾不了可能因为还没领 Pack，现在应重试。
- 需要的 scopes：`meeting:read:meeting_audio`、`meeting:read:meeting_transcript`、`meeting:update:participant_rtms_app_status`（REST 启动用）；事件 rtms_started/stopped 已加过（3 events ✓）。
- **REST 启动的额外硬前提（路径 A）**："Your app can start RTMS for a participant when BOTH: ① participant 是会议 invitee（邀请列表成员，非链接加入者）；② 该 participant 已入会"。82083484374 的 meeting_invitees 为空 → 即使 13262 修好 REST 启动也会被这条挡。修复 = 创建/PATCH 会议时把 host 写进 invitees。**故 Auto-start（路径 B）优先**，无此限制。
- scope 加好 → 回 Account Settings → Zoom Apps → Add app 里选 app（等 1-2 分钟传播）→ 顺便加进 Auto-start。

### 18:50 突破：13262 已消失，授权通过 ✅
- 用户发来 app Scopes 截图：**所有 RTMS scopes 其实都已存在**——`meeting:read:meeting_audio`/`meeting_transcript` 在 Optional 列**未勾选是正常的**（该列是"可选开关"，RTMS 媒体 scope 强制必选；之前"勾不了"就是试图设为可选）。另有 `meeting:update:participant_rtms_app_status`、`rtms:read:rtms_started/stopped/zcc_voice_rtms_stopped`、`meeting:read:meeting`、`meeting:write:meeting`。
- 用 `.local/decode-scope.mjs`（新探针：解密 token + 解码 JWT scope + 重测）实测：**之前 403/13262 → 现在 400/3000 "Meeting 82083484374 has not started"**——授权校验已通过，只剩"会议未开始"。生效原因：用户开了 "Share realtime meeting content with apps" 总开关 + scopes。
- 存量 token 来自 PKCE public client（iss=`zm:cid:SNF7ysbnQ3ux7bGFvrERmA`），JWT scope claim 为空（用户级 token 正常现象，不影响）。
- **下一步**：开一场会 → webhook `meeting.started` → `requestStream()` 自动 PATCH → 预期 `rtms_started` → SDK join → 入转写行。REST 路径潜在残留阻塞：meeting_invitees 为空（若失败则需把 host 写进 invitees 或走 Auto-start）。
- 探针脚本：`.local/test-rtms.mjs`（probe+refresh）、`.local/decode-scope.mjs`（decode JWT scope+probe）。

### 19:00 重要更正：RTMS 是纯 credits 模式，Basic 账号可用 ✅
- **重新核实官方文档**（developers.zoom.us/docs/rtms + zoom.us/pricing/developer）：RTMS 现在**只要求账户有 credits**（"To use RTMS, you'll need credits on your account"），**不再要求付费会议计划**。之前引用的"Plan 表 Basic→Not supported"是旧文档/旧模式，已纠正。Developer Pack Free Trial 20 credits（2026-09-15 ~ 2027-09-15）就是开通凭据，用户判断正确。
- **0% used 是必然**：credits 只在流真正运行时扣费，13262 把每次启动都拦在门外，流从未存在。
- **唯一剩余卡点 = 13262 字面义**：app 不在内容访问授权列表（user-level vs account-level 安装差异）。用户的 app 是 Development 状态，三个点菜单无 "Open App Listing Page"/"Install for Others"。
- **未走完的免费修复**：Marketplace → Manage → 左侧 **Apps on Account** → HireOS Interview → Users & Groups → +Add Users → All users → Allow → 登出重登 → Account Settings → Zoom Apps 的授权列表选 app。
- **会中快速验证法**：真实 Zoom 客户端开会时 Host tools → "Share realtime meeting content with apps" 手动开启 → Zoom 直接推 rtms_started → 后端收流入库（绕过 REST 13262 路径，验证后端全管线）。该菜单若不存在/置灰则 app 未被识别为 RTMS 应用。
- Otter.ai 结论：官方 Zoom 集成（Live Notes/云录制同步）同样需要 Zoom 付费计划，Basic 上只有"本机麦克风录音转录"（设备级，绕过 Zoom）可用——无法用于验证账号 RTMS entitlement。

### 19:15 最终定位：两层问题都坐实了 ✅（需升级 Pro 才能通）
- **发现 1：13262 绑定到具体 client_id**。内容访问授权跟着 OAuth 授权走的客户端走。对照实验（`.local/probe-two-clients.mjs`）：机密客户端 o_OQnk4 启动 → 403/13262；**公开客户端 SNF7ys（PKCE 授权用）启动 → HTTP 204 接受**。会议进行中实测。
- **发现 2：204 ≠ 流启动**。两次 204 后 rtms_started webhook 始终不来（ngrok inspector 确认无请求）。官方 troubleshooting 精确匹配此症状："webhook never fires after starting RTMS"→ 原因 "Account type does not support RTMS (Basic accounts)"；Plan compatibility 表：**Basic (Free) = Not supported，"RTMS webhooks will not fire"**；Pro+ + Developer Pack 才支持。之前"纯 credits 模式"的更正不完整——credits 是计费层，**账号计划仍是硬前提**。
- 之前 13262/3000/204 的飘移 = 不同校验层在不同阶段拦截。
- **修复路径**：升级 Zoom Pro（月付可取消，免费 credits 继续有效）→ 无需改 Zoom 配置，开会即应全链路通（meeting.started → 自动启动 → rtms_started → 收流入库）。用 SNF7ys 启动已无 13262 阻碍；若 signaling 握手（用 o_OQnk4+secret）因 client 不匹配失败，则改后端 authorize() 用机密客户端授权码流程（需固定 redirect 端口 + app 里注册）。
- 不升级的替代：前端已有模拟转写模式可先跑产品。
- 探针脚本累积：test-rtms.mjs、decode-scope.mjs、probe-two-clients.mjs、probe-invitee.mjs（invitee 接口对 live meeting 返 2300 不可用）。
