# Copy to postgres.env.production (gitignored) and fill in a real password
# before `docker compose up`. This is the superuser the container boots
# with; every subsystem's own .env.production DATABASE_URL should use this
# same POSTGRES_USER/POSTGRES_PASSWORD against the `postgres` service.
POSTGRES_USER=hireos
POSTGRES_PASSWORD=
POSTGRES_DB=hireos
